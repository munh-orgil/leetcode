#include<bits/stdc++.h>

using namespace std;

class Solution {
public:
    Solution() {
        ios_base::sync_with_stdio(false);
        cin.tie(NULL);
        cout.tie(NULL);
    }
    string makeGood(string s) {
        string ret = "";
        int n = s.size();
        for (int i = 0; i < n - 1; i++) {
        }
        return ret;
    }
};